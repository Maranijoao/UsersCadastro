import { Routes } from '@angular/router';
import { ResetPasswordModernReversedComponent } from 'app/modules/auth/reset-password/reset-password.component';

export default [
    {
        path     : '',
        component: ResetPasswordModernReversedComponent,
    },
] as Routes;
